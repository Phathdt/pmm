services:
  redis-db:
    image: redis:7.2-alpine
    ports:
      - '16379:6379'

  api-server:
    build:
      dockerfile: ./apps/api-server/Dockerfile
    environment:
      APP_PORT: ${APP_PORT}
      NODE_ENV: ${NODE_ENV}
    ports:
      - '3000:3000'
